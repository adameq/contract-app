# React Frontend Environment Configuration
# This file is a template - DO NOT commit actual values to Git
# For deployment, these values are managed through GitHub Secrets

# Node Environment
NODE_ENV=production

# Make.com Webhook Configuration
# Webhook URL from Make.com scenario
VITE_MAKE_WEBHOOK_URL=

# API Key for Make.com webhook authentication
# Add this key to your Make.com webhook settings (x-make-apikey header)
VITE_MAKE_API_KEY=

# API endpoint for backend services (unified-company-data-server)
# NOTE: Currently not used - form submits directly to Make.com webhook
# Default: http://localhost:3001 (matches fallback in code)
# Production: Set to your API domain (e.g., https://api.yourdomain.com)
VITE_BACKEND_API_URL=http://localhost:3001

# API authentication for unified-company-data-server
# Use a secure key in production (minimum 32 characters)
VITE_BACKEND_API_KEY=test-api-key-for-development-at-least-32-characters-long

# Pipedrive Integration Configuration
# NOTE: These values are NOT used in frontend code - they are for Cloudflare Functions only
# For Cloudflare Pages deployment, set these in Cloudflare Dashboard → Settings → Environment Variables
# For local development with Wrangler, copy these to .dev.vars file (see .dev.vars.example)

# Feature flag - Enable/disable Pipedrive validation
# Set to 'true' to enforce PID validation, 'false' to allow free access
VITE_PIPEDRIVE_ENABLED=false

# The following variables are used ONLY in Cloudflare Functions (not exposed to client):
# PIPEDRIVE_API_URL=https://api.pipedrive.com/v1
# PIPEDRIVE_API_TOKEN=your-pipedrive-api-token-here
# PIPEDRIVE_CUSTOM_FIELD_KEY=f65fca61a8ac7eef5757b18f3e1a15739901c529
# PIPEDRIVE_CUSTOM_FIELD_VALUE=234

# Additional Vite environment variables can be added here
# All variables must be prefixed with VITE_ to be exposed to the client
# See: https://vitejs.dev/guide/env-and-mode.html
